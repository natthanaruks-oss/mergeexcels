# MergeExcels v3.3.4 — Thai Sara Am Reconstruction Patch

## Scope
- เมนู 06 PDF → Excel เท่านั้น
- แก้ PDF ที่แยกสระ `ำ` เป็น `า` และนิคหิต `ํ` คนละ text item

## Fixes
- นำ glyph `ํ` ที่ลอยออกจากคำกลับไปจับกับ `า` ที่อยู่ใกล้ที่สุดตามพิกัด PDF
- รองรับทั้งลำดับ Unicode `ํา` และ `าํ` แล้วรวมเป็น `ำ`
- ป้องกัน glyph combining mark หลุดไปสร้างคอลัมน์แปลก ๆ ใน Excel
- เพิ่ม Regression Test สำหรับ `บารุง → บำรุง` และ `กาหนด → กำหนด`

## Safety
- ซ่อมเฉพาะนิคหิตเดี่ยวที่พบ `า` ใกล้เคียงทางเรขาคณิตอย่างมั่นใจ
- ไม่เดาแก้ตัวอักษรอื่น และไม่เปลี่ยน Logic เมนู 01–05, 07–08
