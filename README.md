# MergeExcels

Static Excel merge, combine, and split tool for Cloudflare Workers Static Assets.

## Cloudflare Workers Builds

- Build command: leave blank
- Deploy command: `npx wrangler deploy`
- Root directory: `/`

The Worker project name must be `mergeexcels` to match `wrangler.jsonc`.
